import { ShoppingCartView } from 'src/sections/checkout/view';

export const metadata = {
  title: 'Shopping Cart | Liburdulu.id',
};

export default function CartPage() {
  return <ShoppingCartView />;
}
