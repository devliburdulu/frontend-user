import { KebijakanPrivasiView } from 'src/sections/footer/view';

export const metadata = {
  title: 'Kebijakan Privasi - Liburdulu',
};

export default function KebijakanPrivasiPage() {
  return <KebijakanPrivasiView />;
}
